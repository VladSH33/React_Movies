import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <header className='header'>
            <a href="#" className="logo">React Movies</a>
            <a href="!#" className="repo">repo</a>
        </header>
    );
};

export default Header;